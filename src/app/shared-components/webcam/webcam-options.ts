/**
 * WebcamComponent options structure interface
 */
export interface WebcamOptions {
    video: boolean | any;
    cameraType: string;
    audio: boolean;
    width: number;
    height: number;
   }
